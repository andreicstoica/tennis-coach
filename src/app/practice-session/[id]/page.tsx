//import { useRouter } from "next/router"

// type Session = {
//   warmup: string;
//   drill: string;
//   game: string;
// };

export default function PracticeSession() {
  // TODO
  // have the chat load, with the auto populated focus as the first message,
  // and the practice session generative UI card there, and a reminder to rate the session
}
